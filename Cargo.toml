[package]
name = "yuiop"
version = "0.1.0"
edition = "2021"
description = "The last package manager you need to know. Universal wrapper over brew, apt, pacman."

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"

[[bin]]
name = "yuiop"
path = "src/main.rs"